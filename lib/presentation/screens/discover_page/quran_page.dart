import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class QuranPage extends StatelessWidget {
  const QuranPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Quran Page"),
    );
  }
}